# 작업 내역 (2025-11-02)

## 계약 상세 화면 정비
- Expo 레이아웃을 참고해 `ContractDetailScreen` 전체 구조를 재작성했습니다.
  - 상단 SafeArea + 뒤로가기 버튼, 히어로 카드, 진행 타임라인, 갑/을 정보, 계약 개요 등 UI 재구성
  - 서명 재전송/링크 복사/웹 PDF 안내 버튼을 Expo와 유사하게 배치
  - 서명 이미지 URL/데이터 URL/상대 경로를 처리해 미리보기 표시
  - `metadata.templateRawContent`와 `templateFormValues`를 이용해 placeholder를 치환한 HTML 미리보기만 노출
- `ContractRepository`에 단건 조회 API(`fetchContractDetail`) 추가, 홈/목록에서 상세 화면으로 네비게이션 연결
- `main.dart`에서 `usePathUrlStrategy()`를 앱 초기화 직후 1회만 호출하도록 정리해 Hashless URL 유지
- 계약 상세 텍스트 카드(`_buildDetailsCard`) 제거로 Expo와 동일하게 미리보기 위주 구성

## 남은 확인 사항 / TODO
- 실제 API 응답에서 서명이 등록되면 이미지가 정상 노출되는지 확인 필요
- HTML 미리보기 내 placeholder가 남는 케이스가 있는지 모니터링
- 추가 요구 사항 있으면 Expo 캡처와 함께 공유 요청 예정

