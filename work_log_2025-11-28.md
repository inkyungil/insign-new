# 작업 내역 - 2025년 11월 28일

## 📋 완료된 작업

### 1. 구독/포인트 시스템 Flutter UI 구현 (완료)

#### 1.1 User 모델 확장 (`lib/models/user.dart`)
- 구독/포인트 필드 추가:
  - `subscriptionTier`: 구독 티어 (free/premium)
  - `monthlyContractLimit`: 월 계약서 제한
  - `contractsUsedThisMonth`: 이번 달 사용량
  - `lastResetDate`: 마지막 리셋 날짜
  - `points`: 보유 포인트
  - `monthlyPointsLimit`: 월 포인트 제한
  - `pointsEarnedThisMonth`: 이번 달 적립 포인트
  - `lastCheckInDate`: 마지막 출석 체크 날짜
- Helper 메서드 추가:
  - `canCreateContract`: 계약서 작성 가능 여부
  - `remainingFreeContracts`: 남은 무료 계약서 수
  - `canCheckInToday`: 오늘 출석 가능 여부

#### 1.2 AuthRepository API 추가 (`lib/data/auth_repository.dart`)
- `getUserStats(token)`: 사용자 통계 조회
- `checkIn(token)`: 출석 체크
- `addPointsFromAd(token, points)`: 광고 포인트 (추후 사용)

#### 1.3 홈 화면 구독 통계 위젯 (`lib/features/home/view/home_screen.dart`)
- 구독 통계 카드 추가:
  - 플랜 배지 (무료/프리미엄)
  - 보유 포인트 표시
  - 이번 달 사용량 (2/4개)
  - 남은 무료 계약서 수
  - 제한 초과 시 안내 메시지
- Pull-to-refresh로 실시간 업데이트

#### 1.4 프로필 화면 구독 카드 (`lib/features/profile/view/profile_screen.dart`)
- 구독 정보 카드:
  - 상세 사용 현황
  - 출석 체크 버튼
  - 오늘 출석 완료 시 버튼 비활성화
  - 출석 성공 시 통계 자동 새로고침
- 상단 우측 알림 아이콘 추가

#### 1.5 계약서 작성 제한 (`lib/features/contracts/view/create_contract_screen.dart`)
- 제출 전 자동 제한 확인:
  - 무료 계약서 소진 + 포인트 부족 → 안내 다이얼로그 + 출석 유도
  - 무료 계약서 소진 + 포인트 충분 → 포인트 사용 확인 다이얼로그
  - 사용 동의 시 계약서 작성 진행

### 2. 이벤트 탭 추가 및 UI 개편 (완료)

#### 2.1 이벤트 화면 생성 (`lib/features/events/view/events_screen.dart`)
- 출석 체크 카드:
  - Gradient 디자인 (보라색 → 파란색)
  - 보유 포인트 / 이번 달 적립 표시
  - 출석 체크 버튼 (하루 1회)
  - 즉시 피드백 (성공/실패 Snackbar)
- 다가오는 이벤트 카드 (향후 확장용)
- 상단 우측 알림 아이콘

#### 2.2 하단 탭 변경 (`lib/app.dart`)
```
변경 전: 홈 | 계약 | 템플릿 | 메시지함 | 마이
변경 후: 홈 | 계약 | 템플릿 | 이벤트🎉 | 마이
```
- 아이콘: `Icons.celebration_outlined`
- 라벨: "이벤트"

#### 2.3 라우팅 재구성 (`lib/core/router/app_router.dart`)
- `/events` → EventsScreen (하단 탭, Shell Route)
- `/inbox` → InboxScreen (full-screen, 알림 아이콘에서 접근)

### 3. 출석체크 백엔드 실제 구현 (완료)

#### 3.1 DB 마이그레이션 실행
```sql
ALTER TABLE users
ADD COLUMN monthly_contract_limit INT NOT NULL DEFAULT 4,
ADD COLUMN contracts_used_this_month INT NOT NULL DEFAULT 0,
ADD COLUMN last_reset_date DATE NULL,
ADD COLUMN points INT NOT NULL DEFAULT 12,
ADD COLUMN monthly_points_limit INT NOT NULL DEFAULT 12,
ADD COLUMN points_earned_this_month INT NOT NULL DEFAULT 0,
ADD COLUMN last_check_in_date DATE NULL;
```

#### 3.2 Entity 업데이트 (`src/users/user.entity.ts`)
- TypeORM 컬럼 데코레이터 추가
- 모든 구독/포인트 필드 매핑

#### 3.3 UsersService 실제 로직 구현 (`src/users/users.service.ts`)

**checkIn(userId)** - 출석 체크
```typescript
- 사용자 조회
- 오늘 출석 여부 확인 (lastCheckInDate)
- 월 포인트 제한 확인 (pointsEarnedThisMonth >= monthlyPointsLimit)
- 통과 시:
  - points += 1
  - pointsEarnedThisMonth += 1
  - lastCheckInDate = today
  - DB 저장
- 응답: { success, points, message }
```

**getUserStats(userId)** - 사용자 통계
```typescript
- 사용자 조회
- 반환: {
    subscriptionTier,
    contractsUsedThisMonth,
    monthlyContractLimit,
    points,
    pointsEarnedThisMonth,
    monthlyPointsLimit
  }
```

**canCreateContract(userId)** - 계약서 작성 가능 여부
```typescript
- 사용자 조회
- 프리미엄: 무제한 허용
- 무료:
  - contractsUsed < limit → 허용
  - points >= 3 → 허용 (포인트 사용)
  - 둘 다 아니면 → 거부
```

#### 3.4 NestJS 서버 재시작
- 변경사항 적용 완료

---

## 🎯 다음 작업 계획

### 우선순위 1: 월별 리셋 기능 구현
**목적**: 매월 1일 자동으로 사용량/포인트 리셋

**구현 방법**:
1. **Cron Job 추가** (`@nestjs/schedule`)
   ```typescript
   @Cron('0 0 1 * *') // 매월 1일 00:00
   async resetMonthlyUsage() {
     await this.usersRepository.update({}, {
       contractsUsedThisMonth: 0,
       pointsEarnedThisMonth: 0,
       points: 12,
       lastResetDate: new Date()
     });
   }
   ```

2. **로그인 시 체크**
   - 사용자 로그인 시 lastResetDate 확인
   - 이번 달이 아니면 리셋 실행

**파일 수정**:
- `nestjs_app/src/app.module.ts`: ScheduleModule 추가
- `nestjs_app/src/users/users.service.ts`: resetMonthlyUsage() 메서드
- `nestjs_app/package.json`: `@nestjs/schedule` 의존성 추가

---

### 우선순위 2: 계약서 작성 시 사용량 증가
**목적**: 계약서 작성 완료 시 contractsUsedThisMonth 증가

**구현 위치**: `nestjs_app/src/contracts/contracts.service.ts`

**로직**:
```typescript
async createContract(userId, data) {
  // 1. 계약서 생성 가능 여부 확인
  const check = await this.usersService.canCreateContract(userId);
  if (!check.canCreate) {
    throw new BadRequestException('계약서 작성 제한 초과');
  }

  // 2. 계약서 생성
  const contract = await this.contractsRepository.save(...);

  // 3. 사용량 증가
  const user = await this.usersRepository.findOne({ where: { id: userId } });

  // 무료 계약서 소진 시 포인트 차감
  if (user.contractsUsedThisMonth >= user.monthlyContractLimit) {
    user.points -= 3;
  }

  user.contractsUsedThisMonth += 1;
  await this.usersRepository.save(user);

  return contract;
}
```

---

### 우선순위 3: 관리자 기능 확장

#### 3.1 사용자별 포인트 관리
**경로**: `nestjs_app/views/admin/users.ejs`

**추가 기능**:
- 포인트 수동 지급/차감 버튼
- 구독 티어 변경 (free ↔ premium)
- 월 사용량 초기화

#### 3.2 전체 통계 대시보드
**경로**: `nestjs_app/views/admin/dashboard.ejs`

**표시 정보**:
- 전체 사용자 수
- 프리미엄 사용자 수
- 오늘 출석 체크 사용자 수
- 이번 달 총 계약서 작성 수
- 총 포인트 지급량

**SQL 쿼리**:
```sql
-- 프리미엄 사용자 수
SELECT COUNT(*) FROM users WHERE subscription_tier = 'premium';

-- 오늘 출석 사용자
SELECT COUNT(*) FROM users WHERE last_check_in_date = CURDATE();

-- 이번 달 계약서 수
SELECT SUM(contracts_used_this_month) FROM users;
```

---

### 우선순위 4: 프리미엄 결제 시스템

#### 4.1 결제 모듈 선택
**옵션**:
- **토스페이먼츠**: 한국 최적화, 간편결제
- **아임포트**: 다양한 PG 지원
- **Stripe**: 글로벌 지원

**추천**: 토스페이먼츠 (국내 서비스)

#### 4.2 구현 단계
1. 토스페이먼츠 가입 및 API 키 발급
2. NestJS 결제 모듈 생성:
   ```
   nestjs_app/src/payments/
   ├── payments.module.ts
   ├── payments.controller.ts
   ├── payments.service.ts
   └── dto/
       ├── create-payment.dto.ts
       └── payment-callback.dto.ts
   ```
3. Flutter 결제 페이지:
   ```
   insign_flutter/lib/features/subscription/
   ├── view/
   │   ├── subscription_plans_screen.dart
   │   └── payment_screen.dart
   └── widgets/
       └── plan_card.dart
   ```
4. 결제 완료 후 subscription_tier 업데이트

---

### 우선순위 5: 추가 이벤트 기능

#### 5.1 광고 포인트 적립
**프론트엔드**: `insign_flutter/lib/features/events/view/events_screen.dart`
- 광고 시청 카드 추가
- Google AdMob 연동
- 시청 완료 → API 호출 → 포인트 +1

**백엔드**: 이미 구현됨
- `POST /api/auth/add-points-from-ad`
- `UsersService.addPointsFromAd(userId, points)`

#### 5.2 친구 초대 이벤트
**테이블 추가**:
```sql
CREATE TABLE referrals (
  id INT PRIMARY KEY AUTO_INCREMENT,
  referrer_id INT NOT NULL,
  referee_id INT NOT NULL,
  referral_code VARCHAR(20) NOT NULL,
  points_earned INT DEFAULT 0,
  created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
  FOREIGN KEY (referrer_id) REFERENCES users(id),
  FOREIGN KEY (referee_id) REFERENCES users(id)
);
```

**보상**:
- 초대한 사람: +5 포인트
- 가입한 사람: +3 포인트

#### 5.3 특별 프로모션
**이벤트 테이블**:
```sql
CREATE TABLE events (
  id INT PRIMARY KEY AUTO_INCREMENT,
  title VARCHAR(255) NOT NULL,
  description TEXT,
  event_type VARCHAR(50), -- 'points_bonus', 'free_contracts', etc.
  bonus_value INT,
  start_date DATE,
  end_date DATE,
  is_active TINYINT DEFAULT 1
);
```

---

### 우선순위 6: Phase 2 & 3 구현 (장기)

#### Phase 2: 요금제 테이블 분리
**목적**: 코드 수정 없이 플랜 변경 가능

**마이그레이션**: `nestjs_app/migrations/20241127_create_subscription_plans_table.sql`

**변경사항**:
- subscription_plans 테이블 생성
- 관리자 페이지에서 플랜 CRUD
- users.subscription_plan_id로 참조

#### Phase 3: 이력 테이블 추가
**목적**: 감사 추적 및 통계

**테이블**:
- `points_ledger`: 포인트 거래 내역
- `user_subscriptions`: 구독 변경 이력
- `monthly_usage`: 월별 사용량 기록

**마이그레이션**:
- `20241127_subscription_redesign_step1_create_tables.sql`
- `20241127_subscription_redesign_step2_migrate_data.sql`
- `20241127_subscription_redesign_step3_cleanup.sql`

---

## 🛠️ 기술 스택

### 프론트엔드 (Flutter)
- **상태관리**: flutter_bloc (Cubit)
- **라우팅**: go_router
- **HTTP**: http package
- **로컬 저장소**: shared_preferences

### 백엔드 (NestJS)
- **프레임워크**: NestJS 10.x
- **ORM**: TypeORM
- **DB**: MySQL 8.x
- **인증**: JWT
- **일정**: @nestjs/schedule (TODO)

---

## 📝 참고 문서

### 작업 로그
- `work_log_2025-11-27_subscription_points.md` - Phase 1 설계
- `work_log_2025-11-27_subscription_redesign.md` - Phase 3 설계
- `work_log_2025-11-27_subscription_refactoring.md` - Phase 2 설계

### 주요 파일 위치
**Flutter**:
- 모델: `insign_flutter/lib/models/user.dart`
- 홈 화면: `insign_flutter/lib/features/home/view/home_screen.dart`
- 프로필: `insign_flutter/lib/features/profile/view/profile_screen.dart`
- 이벤트: `insign_flutter/lib/features/events/view/events_screen.dart`
- API: `insign_flutter/lib/data/auth_repository.dart`

**NestJS**:
- Entity: `nestjs_app/src/users/user.entity.ts`
- Service: `nestjs_app/src/users/users.service.ts`
- Controller: `nestjs_app/src/api-auth/api-auth.controller.ts`
- Migrations: `nestjs_app/migrations/*.sql`

---

## ✅ 테스트 체크리스트

### 출석 체크 기능
- [ ] 로그인 → 이벤트 탭 이동
- [ ] 출석 체크 버튼 클릭
- [ ] "출석 체크 완료! 1포인트가 적립되었습니다" 확인
- [ ] 포인트 12 → 13 증가 확인
- [ ] 버튼이 "오늘 출석 완료!"로 변경 확인
- [ ] 재클릭 시 "오늘은 이미 출석했습니다!" 확인
- [ ] 다음날 다시 출석 가능 확인

### 구독 통계
- [ ] 홈 화면 구독 카드 표시 확인
- [ ] Pull-to-refresh로 업데이트 확인
- [ ] 프로필 화면 구독 정보 확인

### 계약서 작성 제한
- [ ] 계약서 4개 작성 (무료 한도 소진)
- [ ] 5번째 시도 시 다이얼로그 표시
- [ ] 포인트 3개 이상: 포인트 사용 확인
- [ ] 포인트 3개 미만: 안내 + 출석 유도

### 알림 아이콘
- [ ] 홈 화면 상단 우측 종 아이콘
- [ ] 프로필 화면 상단 우측 종 아이콘
- [ ] 이벤트 화면 상단 우측 종 아이콘
- [ ] 클릭 시 메시지함(/inbox) 이동

---

## 🚀 배포 체크리스트

### Flutter 빌드
```bash
cd insign_flutter
flutter clean
flutter pub get
flutter build apk --release
# 또는 Play Store용
flutter build appbundle --release
```

### NestJS 재시작
```bash
cd nestjs_app
npm run build
pm2 restart insign-api
# 또는
sudo systemctl restart insign-api
```

### DB 백업
```bash
mysqldump -u root -p insign > backup_$(date +%Y%m%d).sql
```

---

## 🐛 알려진 이슈

### 해결 완료
- ✅ 출석체크 API가 임시 응답만 반환 → 실제 구현 완료
- ✅ user 테이블에 포인트 필드 없음 → 마이그레이션 실행
- ✅ 백엔드 서버 재시작 필요 → 완료

### 진행 중
- ⏳ 월별 자동 리셋 기능 없음
- ⏳ 계약서 작성 시 사용량 증가 안 됨

### 향후 개선
- 포인트 사용 내역 화면
- 월별 통계 그래프
- 프리미엄 업그레이드 안내

---

## 📞 문의 및 피드백

작업 관련 문의사항이나 버그 발견 시:
1. 이슈 생성: `/home/insign/issues.md`
2. 로그 확인: `nestjs_app/logs/`
3. DB 상태 확인: `mysql -u root -p insign`

---

**작성일**: 2025-11-28
**작성자**: Claude Code
**버전**: v1.0.0
