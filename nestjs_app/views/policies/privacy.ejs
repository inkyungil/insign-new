<!DOCTYPE html>
<html lang="ko">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title><%= title %> | 인싸인</title>
    <style>
      body {
        margin: 0;
        font-family: "Noto Sans KR", -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif;
        background-color: #f7f7f9;
        color: #222;
        line-height: 1.6;
      }
      header {
        background: linear-gradient(135deg, #233765, #3f63d6);
        color: #fff;
        padding: 56px 20px 40px;
        text-align: center;
      }
      header h1 {
        margin: 0;
        font-size: 2rem;
        font-weight: 700;
      }
      header p.meta {
        margin: 12px auto 0;
        max-width: 680px;
        font-size: 0.95rem;
        opacity: 0.85;
      }
      main {
        max-width: 820px;
        margin: -32px auto 60px;
        padding: 32px;
        background-color: #fff;
        box-shadow: 0 18px 40px rgba(35, 55, 101, 0.12);
        border-radius: 16px;
      }
      .content {
        font-size: 1rem;
        color: #333;
      }
      .content h2,
      .content h3,
      .content h4 {
        color: #1d2d50;
        margin-top: 28px;
      }
      .content p {
        margin: 12px 0;
      }
      .content ul,
      .content ol {
        padding-left: 20px;
        margin: 12px 0;
      }
      footer {
        text-align: center;
        font-size: 0.85rem;
        color: #626f92;
        margin-bottom: 40px;
      }
      a.inline-link {
        color: #3f63d6;
        text-decoration: none;
      }
      a.inline-link:hover {
        text-decoration: underline;
      }
    </style>
  </head>
  <body>
    <header>
      <h1><%= title %></h1>
      <p class="meta">
        <span>최종 업데이트: <%= new Date(updatedAt).toLocaleDateString('ko-KR') %></span>
        <% if (version) { %>
        · <span>버전: <%= version %></span>
        <% } %>
      </p>
      <p class="meta">
        이용약관이 필요하신가요? <a class="inline-link" href="/policies/terms">여기를 클릭</a>하세요.
      </p>
    </header>
    <main>
      <div class="content">
        <%
          const isHtmlContent = /<[a-z][\s\S]*>/i.test(content);
          const rendered = isHtmlContent ? content : content.replace(/\n/g, '<br />');
        %>
        <%- rendered %>
      </div>
    </main>
    <footer>© <%= new Date().getFullYear() %> 인싸인. All rights reserved.</footer>
  </body>
</html>
