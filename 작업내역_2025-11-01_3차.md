# 작업 내역 (2025-11-01 19:20)

## 완료한 내용
- 템플릿 기반 계약 작성 흐름을 Expo와 동일한 4단계(step) 구조로 재구성했습니다.
- 템플릿 `formSchema`를 파싱하기 위한 모델(`TemplateFormSchema`, `TemplateFormSection`, `TemplateFieldDefinition` 등)을 추가했습니다.
- 템플릿 작성 단계에서 계약 항목 입력 UI를 타입별(텍스트, 숫자, 날짜, 선택, 체크박스, 라디오 등)로 동적 생성하고 검증하도록 구현했습니다.
- 요약 단계에 템플릿 항목 요약과 서명자가 입력할 필드 안내를 노출하고, 샘플 JSON 노출을 모두 제거했습니다.
- `MemberInfoScreen`을 생성해 기본 정보/비밀번호 변경 폼을 제공하고, 프로필·설정 화면에서 해당 페이지로 이동하도록 라우팅을 추가했습니다.

## 남은 TODO
- 템플릿 필드 입력 상태를 저장/복원하거나 서버에 제출하는 실제 API 규격을 확정해야 합니다.
- 서명(직접 그리기/이미지 업로드) 기능은 Expo 수준으로 구현하지 않았으므로 추후 연결이 필요합니다.
- 템플릿 수행자(서명자) 전용 섹션 렌더링과 실제 서명 요청 API 연동을 검토해야 합니다.
- 윈도우 환경에서 `flutter format`이 실행되지 않는 `bash\r` 이슈를 해결하고 포맷/정적 분석을 실행해야 합니다.
